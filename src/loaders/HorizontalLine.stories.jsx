import React from "react";

import { HorizontalLine } from "./HorizontalLine";

export default {
  title: "Loaders/HorizontalLine",
  component: HorizontalLine,
};

const Template = (args) => <HorizontalLine {...args} />;

export const Default = Template.bind({});
